# read in the file, eventually this can be a database
with open("./datastore.csv") as file:
    print(file.readline())

# create the data structure from the file

# render the UI, maybe this can be in flask so it is a web UI

# ask user for option, given the amount of free time they have left

# randomly generate an activity from the database

# update the data structure or database to make sure this activity is checked up on later, if the right amount of time has elapsed
